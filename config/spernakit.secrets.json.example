{
	"_comment": "Split-secrets file template. Copy to config/spernakit.secrets.json (gitignored) and replace placeholders. Keys are dot-path addressable from *Ref config fields and getSecret(); keep this example's key shape in step with the live file (scripts/check-secrets-shape.ts).",
	"oauth": {
		"github": {
			"clientSecret": "replace-me"
		},
		"google": {
			"clientSecret": "replace-me"
		},
		"microsoft": {
			"clientSecret": "replace-me"
		}
	}
}
