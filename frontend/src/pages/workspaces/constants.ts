import { WORKSPACE_ROLES, type WorkspaceMemberRole } from 'spernakit-shared';

const WORKSPACE_ROLE_OPTIONS: WorkspaceMemberRole[] = Object.values(WORKSPACE_ROLES);

export { WORKSPACE_ROLE_OPTIONS };
