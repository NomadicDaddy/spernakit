# Spernakit fleet manifest (example)
#
# Copy this file to spernakit.psd1 and register your apps. The real
# spernakit.psd1 is gitignored so your fleet roster stays local.
#
# Each entry declares a spernakit-based application in a sibling directory of this
# repository ({workspace-root}/{slug}/, where {workspace-root} is spernakit's parent).
# Used by reset.ps1, CLI aliases, and other orchestration scripts.
#
# Field semantics:
#   frontendPort        - Vite dev-server port owned by this app
#   backendPort         - Elysia API server port owned by this app
#   appName             - Human-readable display name
#   description         - One-line purpose statement
#   version             - App-owned semver (independent of the template version)
#   spernakit_version   - Exact template semver this app has been synced to
#
# Every field above is derived, not authoritative. Each app's package.json owns version and
# spernakit_version; its config/<slug>.json owns the name, description, and port pair. Run
# `bun run fleet-manifest:sync` to restate this file from them, and `bun run check:fleet-manifest`
# to confirm it still matches.
#
@{
	ExpectedConfigs = @{
		'spernakit' = @{
			appName      = 'Spernakit v3'
			backendPort  = 3331
			description  = 'Self-Hosted Multi-User Application Template'
			frontendPort = 3330
			version      = '3.30.0'
		}
	}
}
